import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
        type: "dark",
        primary: {
            main: '#29b6f6',
        },
        secondary: {
            main: '#01579b',
        },
    }
});

export default theme;